name: my_flutter_web_app
description: Aplicativo para gerenciamento de licitações da Prefeitura de Arapiraca.

# Versão do seu aplicativo
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts:
  intl:
  pdf: ^3.11.0
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  provider: ^5.0.0
  logger: ^2.3.0
  crypto:
  open_file: ^3.2.1
  table_calendar:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/hudson.png
    - assets/melania.png